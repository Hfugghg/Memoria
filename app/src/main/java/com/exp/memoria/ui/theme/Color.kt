package com.exp.memoria.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * [应用颜色定义]
 *
 * 这个文件定义了应用 UI 所使用的主要颜色调色板。
 * 这些颜色被用于定义亮色主题和暗色主题的 `ColorScheme`。
 *
 * 遵循 Material 3 设计规范，通常会为亮色和暗色主题定义不同的颜色值。
 * - `*80` 后缀的颜色（如 Purple80）通常是为暗色主题设计的，它们更亮、更柔和。
 * - `*40` 后缀的颜色（如 Purple40）通常是为亮色主题设计的，它们更深、更饱和。
 */

// 暗色主题颜色
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

// 亮色主题颜色
val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)
